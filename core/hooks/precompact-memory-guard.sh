#!/usr/bin/env bash
# H19 — retired compatibility placeholder.
# Context compaction is not a memory-rotation boundary. The raw transcript and
# the independent offline ingest process are the durable recovery path.
cat >/dev/null
exit 0
